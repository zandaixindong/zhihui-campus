<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhihui.system.mapper.SysMenuMapper">

    <resultMap id="BaseResultMap" type="com.zhihui.system.entity.SysMenu">
        <id column="id" property="id"/>
        <result column="menu_name" property="menuName"/>
        <result column="parent_id" property="parentId"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="path" property="path"/>
        <result column="component" property="component"/>
        <result column="menu_type" property="menuType"/>
        <result column="perms" property="perms"/>
        <result column="icon" property="icon"/>
        <result column="visible" property="visible"/>
        <result column="status" property="status"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 根据用户ID查询菜单列表 -->
    <select id="selectMenusByUserId" resultMap="BaseResultMap">
        SELECT DISTINCT m.* FROM sys_menu m
        LEFT JOIN sys_role_menu rm ON m.id = rm.menu_id
        LEFT JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId} AND m.status = 1
        ORDER BY m.sort_order
    </select>

    <!-- 根据角色ID查询菜单列表 -->
    <select id="selectMenusByRoleId" resultMap="BaseResultMap">
        SELECT m.* FROM sys_menu m
        LEFT JOIN sys_role_menu rm ON m.id = rm.menu_id
        WHERE rm.role_id = #{roleId} AND m.status = 1
        ORDER BY m.sort_order
    </select>

    <!-- 查询所有菜单列表 -->
    <select id="selectMenuAll" resultMap="BaseResultMap">
        SELECT * FROM sys_menu WHERE status = 1 ORDER BY sort_order
    </select>

</mapper>
